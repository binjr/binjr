version: '{build}'

environment:
  GITHUB_TOKEN:
    secure: 4vpPlabt2OKA/pyJKSZUr3KJg8y0uJFnzf/kTmVUTCPIb9Q3fpjED+1LEXHYQ4UY
  SCM_USERNAME:
    secure: 8BjoFIdSfcFM5UNtivbS5w==

branches:

  only:

  - master

build_script:

- cmd: >-

    set BINJR_BUILD_NUMBER=%APPVEYOR_BUILD_NUMBER%

    git clone -b travis `git config --get remote.origin.url` binjr/target/travis

    cd binjr

    mvn clean deploy --settings "./target/travis/settings.xml" -P buildNativeBundles